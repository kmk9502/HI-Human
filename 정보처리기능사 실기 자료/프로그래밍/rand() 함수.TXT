문제 : 1 부터 70 사이의 랜덤한 값을 얻을 때 필요한 라이브러리 함수를 입력하시오.(5점)

#include<stdio.h>

int main() {

    for (int i = 0; i < 10; i++) {

	printf("%d\n", (1 + (  )%70)); // 70으로 나누므로 나머지는 70 미만이 될 수밖에 없다. 71%70 = 1, 88%70 = 18
				       // + 1을 하는 이유는 69%70 = 69, 70%70 = 0 이기 때문에 70이라는 값을 얻기 위해서 사용한다.

    }

}


답 : rand()  // Randum()이 아니라 rand() 이다.